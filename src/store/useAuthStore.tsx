// import create from 'zustand';

// type authStoreType = {
//   isAuth: boolean;
//   setAuth: (connected: boolean) => void;
// };

// export const useAuthStore = create<authStoreType>((set: any, get: any) => ({
//   isAuth: false,
//   setAuth: (connected: boolean): void => {
//     set({ isAuth: connected });
//   },
// }));
export const useAuthStore = () => {
  //
};
